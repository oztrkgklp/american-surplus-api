/** Stored in organization_addresses.address_type (VARCHAR); add values without DB enum migration. */
export enum OrganizationAddressType {
    HEADQUARTERS = 'HEADQUARTERS',
    MAILING = 'MAILING',
    OFFICE_LOCATION = 'OFFICE_LOCATION',
}
