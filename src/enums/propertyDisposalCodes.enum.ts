/**
 * Property disposal condition codes used across the system.
 */
export enum PropertyDisposalName {
    N = 'New/Unused',
    U = 'Usable',
    R = 'Repairable',
    X = 'Salvage',
    S = 'Scrap',
}