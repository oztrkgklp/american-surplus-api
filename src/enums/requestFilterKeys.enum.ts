export enum RequestFilterKeys {
  ID = 'id',
  TCN = 'tcn',
  STATUS = 'status',
  CREATED_AT = 'createdAt',
  UPDATED_AT = 'updatedAt',
  ORGANIZATION = 'organization',
  DONE_ACCOUNT = 'donee_account',
  REQUESTOR = 'requestor',
  ALLOCATED_DATE = 'allocated_date',
}
